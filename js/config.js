GameConfig = {
    width: 960,
    height: 540,

    restNodeEndpoint: window.location.origin + ":3000/",
    restPhpEndpoint: window.location.href + 'server.php/',

    playerMovementSpeed: 150,
    playerJumpHeight: 350,

    gravity: 400,
};